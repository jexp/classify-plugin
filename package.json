{
  "name": "classify-plugin",
  "version": "0.1.2",
  "description": "Claude Code plugin: classify every line sent to the LLM by category, source (human/agent) and noise, with persistent SQLite stats.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jexp/classify-plugin.git"
  },
  "type": "module",
  "files": [
    "src",
    "scripts",
    "hooks",
    "commands",
    ".claude-plugin"
  ],
  "engines": {
    "node": ">=22.5"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "version": "node scripts/sync-version.js && git add .claude-plugin/plugin.json .claude-plugin/marketplace.json",
    "release": "node scripts/release.js"
  }
}
